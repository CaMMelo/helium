# @name hel_init
# @phase pm
# @expect success
# @command bash -c 'tmp=tests/pm/_tmp_init_test; rm -rf "$tmp"; mkdir -p "$tmp"; build/bin/hel init "$tmp/pkg" >"$tmp/out.txt" 2>&1; rc=$?; cat "$tmp/out.txt"; rm -rf "$tmp"; exit $rc'
# @match Created project|bootstrap
