// @name closure_capture_twice
// @phase codegen
// @expect success

import std.io

mk_adder = (x: i32) {
    (y: i32) {
        x + y;
    };
}

main = () : IO<()> {
    adder = mk_adder(2);
    n = adder(3);
    m = adder(4);
    io.println(f"{n}, {m}");
}
