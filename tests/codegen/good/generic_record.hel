// @name generic_record
// @phase codegen
// @expect success

import std.io

type Box<T> = { value: T };

identity = <T>(x: T): T { x };

main = () : IO<()> {
    io.print_int((identity(Box{ value: 42 })).value)
}
