// @name monad_bind
// @phase stdlib
// @expect success

import std.io

main = () : IO<()> {
    io.prints("A") >>= (_: ()) {
        io.prints("B") >>= (_: ()) {
            io.println("C")
        }
    }
}
